// Рядок з дужками вважається валідним, якщо на кожну дужку, що закриває, є раніше відкрита
// і кожну раніше відкриту є закриває.

// Напишіть функцію parentheses(value) перевіряючий рядок з дужками на валідність.
// parentheses('') === false
// parentheses('()()') === true
// parentheses('(()())') === true
// parentheses('(()') === false
// parentheses(')') === false

// //Напишіть функцію filterFalse(arr), яка очищає масив від помилкових (false)
// //Значень: false, null, undefined, 0, -0, NaN і "" (пустий рядок).

// const array = [NaN, 0, 77, false, -17, "", undefined, 99, null];
